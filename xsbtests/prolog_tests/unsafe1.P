:- import unix/1 from unix.
:- import numbervars/1 from num_vars.

test :-
	p(X,Y), numbervars([X,Y]), writeln(p(X,Y)), fail.
test :- unix('rm unsafe1.O'), writeln('unsafe1.O removed').

p(X1, X2) :-
        p1(Y1, Y2), s(X1, X2, Y1, Y2).

p1(X, X).

s(X, X, [], X).
s([X], X, [], X).
s([X], X, [_], X).

