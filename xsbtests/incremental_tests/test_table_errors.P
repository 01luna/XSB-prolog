:- import put_attr/1 from machine.
:- import incr_assert/1 from increval.
:- import get_error_message/2,get_error_tag/2,get_error_goal/2 from error_handler.

test:- catch(t1(_X),E,(get_error_message(E,M),writeln(message(M)))),fail.
test:- catch(t1(_X),E,(get_error_tag(E,M),writeln(tag(M)))),fail.
test:- catch(t1(_X),E,(get_error_goal(E,M),writeln(goal(M)))),fail.
test.


t1(X):- d(X),Y is X + 1,incr_assert(d(Y)).

:- dynamic d/1 as incremental.
d(1).


:- table p/1 as subsumptive.
p(_X).

test_1:- put_attr(X, ti, v(1)),p(X).

:- table t1/1 as incremental.

