/* This demo shows the use of the meta programming (Hilog features). */

o1[m1(a1) -> r1].
o1[m2(b1,b2) -> r2].
o1[m3 -> r3].
o1[m4(c1,c2,c3) -> r4].

o2[M->R] :-
	o1[M->R],
	M=_F(_A1,_A2).


tst1 :- writeln('all objects, all method invocations:')@prolog(), X[M -> R],
	fmt_write("%S %S %S\n", args(X,M,R))@flora(io), fail.
tst1 :- true.

test :- tell(temp)@prolog(), tst1, told@prolog().

